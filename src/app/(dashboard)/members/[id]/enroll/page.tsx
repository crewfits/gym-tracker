import Link from "next/link";import { requireGym } from "@/lib/auth";import { createMembership } from "@/app/actions/core";import { MembershipForm } from "@/components/membership-form";
export default async function Enroll({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{error?:string}>}){const [{id},p]=await Promise.all([params,searchParams]);const {supabase,gym}=await requireGym();const {data:plans}=await supabase.from("plans").select("*").eq("gym_id",gym.id).eq("is_active",true).order("name");return <><div className="page-head"><div><p className="eyebrow">New membership</p><h1>Enroll member</h1></div><Link className="button secondary" href={`/members/${id}`}>Cancel</Link></div><MembershipForm memberId={id} plans={plans??[]} action={createMembership} error={p.error}/></>}
