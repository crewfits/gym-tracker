import Link from "next/link";import { notFound } from "next/navigation";import { requireGym } from "@/lib/auth";import { formatInr } from "@/lib/domain";import { recordPayment } from "@/app/actions/core";import { Feedback } from "@/components/feedback";
export default async function Pay({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{charge?:string;error?:string}>}){const [{id},p]=await Promise.all([params,searchParams]);if(!p.charge)notFound();const {supabase,gym}=await requireGym();const {data:charge}=await supabase.from("charges").select("*, memberships!inner(member_id,plan_name)").eq("id",p.charge).eq("gym_id",gym.id).single();if(!charge||charge.memberships.member_id!==id)notFound();const {data:payments}=await supabase.from("payments").select("amount_paise,payment_reversals(amount_paise)").eq("charge_id",charge.id).is("voided_at",null);const paid=(payments??[]).reduce((s,x)=>s+Number(x.amount_paise)-x.payment_reversals.reduce((r,item)=>r+Number(item.amount_paise),0),0);const balance=Number(charge.total_paise)-paid;const today=new Date().toISOString().slice(0,10);return <><div className="page-head"><div><p className="eyebrow">{charge.memberships.plan_name}</p><h1>Record payment</h1><p className="muted">Outstanding balance: <strong>{formatInr(balance)}</strong></p></div><Link className="button secondary" href={`/members/${id}`}>Cancel</Link></div><Feedback error={p.error}/><form action={recordPayment} className="card form" style={{maxWidth:700}}><input type="hidden" name="member_id" value={id}/><input type="hidden" name="charge_id" value={charge.id}/><div className="form-grid"><div className="field"><label>Amount (₹) *</label><input type="number" name="amount" min="0.01" max={(balance/100).toFixed(2)} step="0.01" defaultValue={(balance/100).toFixed(2)} required/></div><div className="field"><label>Payment date *</label><input type="date" name="paid_on" defaultValue={today} required/></div><div className="field"><label>Method *</label><select name="method"><option value="cash">Cash</option><option value="upi">UPI</option><option value="card">Card</option><option value="bank_transfer">Bank Transfer</option></select></div><div className="field"><label>Transaction/reference</label><input name="reference"/></div></div><div className="field"><label>Notes</label><textarea name="notes" rows={3}/></div><button className="button" style={{justifySelf:"start"}}>Record and issue receipt</button></form></>}
