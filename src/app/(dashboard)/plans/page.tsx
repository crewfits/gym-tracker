import { createPlan,togglePlan } from "@/app/actions/core";import { requireGym } from "@/lib/auth";import { formatInr } from "@/lib/domain";import { Feedback } from "@/components/feedback";
export default async function Plans({searchParams}:{searchParams:Promise<{success?:string;error?:string}>}){const p=await searchParams;const {supabase,gym}=await requireGym();const {data:plans}=await supabase.from("plans").select("*").eq("gym_id",gym.id).order("is_active",{ascending:false}).order("name");return <><div className="page-head"><div><p className="eyebrow">Catalogue</p><h1>Membership plans</h1><p className="muted">Standardize duration and pricing while keeping overrides available.</p></div></div><Feedback success={p.success} error={p.error}/><div className="grid-2"><section className="card table-wrap"><table className="table"><thead><tr><th>Plan</th><th>Duration</th><th>Fee</th><th>Status</th><th></th></tr></thead><tbody>{(plans??[]).map(x=><tr key={x.id}><td><strong>{x.name}</strong></td><td>{x.duration_value} {x.duration_unit}</td><td>{formatInr(Number(x.default_fee_paise))}</td><td><span className={`badge ${x.is_active?"active":""}`}>{x.is_active?"active":"archived"}</span></td><td><form action={togglePlan}><input type="hidden" name="id" value={x.id}/><input type="hidden" name="active" value={String(!x.is_active)}/><button className="button secondary small">{x.is_active?"Archive":"Activate"}</button></form></td></tr>)}</tbody></table>{!plans?.length&&<div className="empty">Create your first membership plan.</div>}</section><form action={createPlan} className="card form"><h2>Add plan</h2><div className="field"><label>Name</label><input name="name" required placeholder="Monthly"/></div><div className="form-grid"><div className="field"><label>Duration</label><input type="number" name="duration_value" min="1" defaultValue="1" required/></div><div className="field"><label>Unit</label><select name="duration_unit"><option value="months">Months</option><option value="days">Days</option></select></div></div><div className="field"><label>Standard fee (₹)</label><input type="number" name="fee" min="0" step="0.01" required/></div><button className="button">Create plan</button></form></div></>}
