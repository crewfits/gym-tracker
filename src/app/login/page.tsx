import { Dumbbell } from "lucide-react";import { signIn,signUp } from "@/app/actions/auth";import { Feedback } from "@/components/feedback";
export default async function Login({searchParams}:{searchParams:Promise<{error?:string}>}){const p=await searchParams;return <main style={{minHeight:"100vh",display:"grid",placeItems:"center",padding:20,background:"linear-gradient(135deg,#10271c,#1c5136)"}}><div className="card" style={{width:"min(430px,100%)",padding:32}}><div className="brand" style={{color:"var(--ink)",margin:"0 0 28px"}}><span className="brand-mark"><Dumbbell size={20}/></span> GymDesk</div><p className="eyebrow">Owner access</p><h1>Run your gym with clarity.</h1><p className="muted" style={{marginBottom:24}}>Track memberships, collections and renewals from one calm workspace.</p><Feedback error={p.error}/><form className="form"><div className="field"><label>Gym name</label><input name="gymName" placeholder="Momentum Fitness"/></div><div className="field"><label>Email</label><input name="email" type="email" required/></div><div className="field"><label>Password</label><input name="password" type="password" minLength={8} required/></div><button className="button" formAction={signIn}>Sign in</button><button className="button secondary" formAction={signUp}>Create owner account</button></form></div></main>}
