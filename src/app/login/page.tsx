import { Dumbbell } from "lucide-react";import { signIn } from "@/app/actions/auth";import { Feedback } from "@/components/feedback";import { safeReturnPath } from "@/lib/return-path";
export default async function Login({searchParams}:{searchParams:Promise<{error?:string;next?:string}>}){const p=await searchParams;const next=safeReturnPath(p.next);return <main style={{minHeight:"100vh",display:"grid",placeItems:"center",padding:20,background:"linear-gradient(135deg,#10271c,#1c5136)"}}><div className="card" style={{width:"min(430px,100%)",padding:32}}><div className="brand" style={{color:"var(--ink)",margin:"0 0 28px"}}><span className="brand-mark"><Dumbbell size={20}/></span> GymDesk</div><p className="eyebrow">Provisioned owner access</p><h1>Welcome back.</h1><p className="muted" style={{marginBottom:24}}>Sign in with the owner account issued for your gym.</p><Feedback error={p.error}/><form className="form"><input type="hidden" name="next" value={next}/><div className="field"><label>Email</label><input name="email" type="email" autoComplete="email" required/></div><div className="field"><label>Password</label><input name="password" type="password" autoComplete="current-password" minLength={8} required/></div><button className="button" formAction={signIn}>Sign in</button></form><small className="muted">This browser stays signed in on the same website address. A new device, private window, or changed tunnel address needs one sign-in.</small><small className="muted" style={{display:"block",marginTop:8}}>New gym accounts are created by the GymDesk deployment team.</small></div></main>}
